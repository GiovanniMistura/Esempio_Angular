export class Contact {
    id: number; // id del contatto
    number: number; // numero di telefono
    prefix: string; // prefisso del numero
    type: string; // tipo di numero (fisso, cellulare)
    isActive: boolean; // indica se il contatto è attivo online --> non usato
  }